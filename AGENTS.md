# AGENTS.md

Die Projektregeln stehen in `CLAUDE.md` und gelten unverändert auch für Codex.

Diese Datei enthält bewusst keine Kopie davon: Eine zweite Fassung driftet
auseinander, sobald eine der beiden gepflegt wird. Der bisherige Duplikat-Stand
hatte durch eine pauschale Ersetzung bereits erfundene Pfade (`.Codex/` statt
`.claude/`).
